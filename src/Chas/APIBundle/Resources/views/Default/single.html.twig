{% extends '::base.html.twig' %}

{% block body %}
<h1>{{ resources[0].timetable.name }}</h1>
<ul>
{% for resource in resources %}
    <li>
        {{ resource.departure.format('H:i') }} - {{ resource.stop }}
    </li>
{% endfor %}
</ul>
{% endblock %}
